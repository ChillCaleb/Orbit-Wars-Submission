# Orbit-Wars-Submission

Run a local simulation and save/watch an MP4:

```bash
.venv312/bin/python watch_match.py --opponent starter
```

Videos are stored in `MP4/` as `replay 1.mp4`, `replay 2.mp4`, and so on.
